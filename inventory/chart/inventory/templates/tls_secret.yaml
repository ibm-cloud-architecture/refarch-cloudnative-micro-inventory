{{- if and (not .Values.tls.secretName) .Values.tls.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "inventory.fullname" . }}-tls
  labels:
    {{- include "inventory.labels" . | indent 4 }}
data:
  tls.crt: {{ .Values.tls.crt }}
  tls.key: {{ .Values.tls.key }}
type: Opaque
{{- end }}